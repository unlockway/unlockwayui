import 'package:flutter/material.dart';

class NextMeals extends StatelessWidget {
  const NextMeals({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

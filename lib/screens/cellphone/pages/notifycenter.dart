import 'package:flutter/material.dart';

class NotifyCenter extends StatelessWidget {
  const NotifyCenter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/material.dart';

class RoutineEdition extends StatelessWidget {
  const RoutineEdition({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

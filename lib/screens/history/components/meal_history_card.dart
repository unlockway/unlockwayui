import 'package:flutter/material.dart';

class MealHistoryCard extends StatelessWidget {
  const MealHistoryCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(3),
      width: double.infinity,
      child: const Row(),
    );
  }
}
